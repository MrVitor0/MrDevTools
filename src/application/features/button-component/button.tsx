import React from 'react'
import { ButtonProps } from '../../../domain/models/types'

const Button: React.FC<ButtonProps> = ({ ...props }) => {
    return (
        <a>Sample</a>
    )
}

export default Button
